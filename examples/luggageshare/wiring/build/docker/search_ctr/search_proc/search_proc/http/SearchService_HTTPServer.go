// Blueprint: Auto-generated by HTTP Plugin
package http

import (
	"encoding/json"
	"net/http"
	"github.com/gorilla/mux"
	"blueprint/goproc/search_proc/healthcheck"
	"github.com/blueprint-uservices/cldrel_course/luggageshare/workflow"
	"context"
)

type SearchService_HTTPServerHandler struct {
	Service healthcheck.SearchService_HealthChecker
	Address string
}

func New_SearchService_HTTPServerHandler(ctx context.Context, service healthcheck.SearchService_HealthChecker, serverAddress string) (*SearchService_HTTPServerHandler, error) {
	handler := &SearchService_HTTPServerHandler{}
	handler.Service = service
	handler.Address = serverAddress
	return handler, nil
}

// Blueprint: Run is called automatically in a separate goroutine by runtime/plugins/golang/di.go
func (handler *SearchService_HTTPServerHandler) Run(ctx context.Context) error {
	router := mux.NewRouter()
	// Add paths for the mux router
	
	router.Path("/Health").HandlerFunc(handler.Health)
	
	router.Path("/Search").HandlerFunc(handler.Search)
	
	srv := &http.Server {
		Addr: handler.Address,
		Handler: router,
	}

	go func() {
		select {
		case <-ctx.Done():
			srv.Shutdown(ctx)
		}
	}()

	return srv.ListenAndServe()
}


func (handler *SearchService_HTTPServerHandler) Health(w http.ResponseWriter, r *http.Request) {
	var err error
	defer r.Body.Close()
	
	ctx := context.Background()
	ret0, err := handler.Service.Health(ctx)
	if err != nil {
		http.Error(w, err.Error(), 500)
		return
	}
	response := struct {
		
		Ret0 string
		
	}{}
	
	response.Ret0 = ret0
	
	json.NewEncoder(w).Encode(response)
}

func (handler *SearchService_HTTPServerHandler) Search(w http.ResponseWriter, r *http.Request) {
	var err error
	defer r.Body.Close()
	
	color := r.URL.Query().Get("color")
	
	request_length := r.URL.Query().Get("length")
	var length int64
	if request_length != "" {
		err = json.Unmarshal([]byte(request_length), &length)
		if err != nil {
			http.Error(w, err.Error(), 500)
			return
		}
	}
	
	request_breadth := r.URL.Query().Get("breadth")
	var breadth int64
	if request_breadth != "" {
		err = json.Unmarshal([]byte(request_breadth), &breadth)
		if err != nil {
			http.Error(w, err.Error(), 500)
			return
		}
	}
	
	request_height := r.URL.Query().Get("height")
	var height int64
	if request_height != "" {
		err = json.Unmarshal([]byte(request_height), &height)
		if err != nil {
			http.Error(w, err.Error(), 500)
			return
		}
	}
	
	request_price := r.URL.Query().Get("price")
	var price float64
	if request_price != "" {
		err = json.Unmarshal([]byte(request_price), &price)
		if err != nil {
			http.Error(w, err.Error(), 500)
			return
		}
	}
	
	startDate := r.URL.Query().Get("startDate")
	
	endDate := r.URL.Query().Get("endDate")
	
	ctx := context.Background()
	ret0, err := handler.Service.Search(ctx, color, length, breadth, height, price, startDate, endDate)
	if err != nil {
		http.Error(w, err.Error(), 500)
		return
	}
	response := struct {
		
		Ret0 []workflow.LuggageItem
		
	}{}
	
	response.Ret0 = ret0
	
	json.NewEncoder(w).Encode(response)
}

